server:
  port: 7073

spring :
  application :
    name : EligibilityDeterminationService
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jrtp_ish_db
    username: root
    password: Sandeep@123

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

